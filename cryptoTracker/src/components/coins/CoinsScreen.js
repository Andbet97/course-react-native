import React from 'react';
import { View, Text } from 'react-native';

const CoinsScreen = () => {
    return (
        <View>
            <Text>
                Screen Coins
            </Text>
        </View>
    );
};

export default CoinsScreen;
