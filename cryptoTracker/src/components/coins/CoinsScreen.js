import React from 'react';
import { Text } from 'react-native';

const CoinsScreen = () => {
    return (
        <Text>
            Screen Coins
        </Text>
    );
};

export default CoinsScreen;
